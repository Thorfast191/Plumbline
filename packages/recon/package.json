{
  "name": "@plumbline/recon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no linter configured yet)\"",
    "test": "echo \"(no tests yet — see docs/BUILD-SPEC.md for the phase this package belongs to)\""
  },
  "dependencies": {
    "@plumbline/metrics": "workspace:*",
    "@plumbline/connector": "workspace:*",
    "@plumbline/model": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
